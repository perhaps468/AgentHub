# Database configuration
DATABASE_URL=mysql+pymysql://root:password@127.0.0.1:3306/agenthub

# Server configuration
HOST=127.0.0.1
PORT=8088

# Qwen (鍗冮棶) OpenAI-compatible Provider configuration
# P1-1 uses a single built-in Agent (PM Agent) with qwen-plus model
QWEN_API_KEY=your_qwen_api_key_here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-plus

# WebSocket chat output
# false: only send message_start + final message_end(final_content)
# true: also send incremental message_delta events
CHAT_STREAM_OUTPUT_ENABLED=false
