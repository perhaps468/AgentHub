# Analysis and Next Action Decision Point

Progress: Iteration {{ iteration }}/{{ max_iterations }}

## Global Task summary:
```
{{ task_to_solve_summary }}```

## Your Task
1. Analyze the execution result and progress, and decide whether the current step solved part of the task.
2. Determine the most effective next step.
3. Select exactly ONE tool from the available list.
4. Use variable interpolation where it reduces repetition.

## Tool protocol
{{ tools_prompt }}

## Variable protocol
{{ variables_prompt }}

## Response Requirements
Provide exactly two raw XML blocks:
1. Your analysis in `<thinking>` tags. Do not include `<context_analysis/>`.
2. Your tool execution in one `<action>` block containing exactly one tool tag.

## Last executed action result
Last executed tool {{ last_executed_tool }} Execution Result:

<{{ variable_name }}>
{{ response_display }}
</{{ variable_name }}>

## Response Format
<thinking>
[Detailed analysis of progress and reasoning for the next step]
</thinking>

<action>
<selected_tool_name>
  <argument_name>value</argument_name>
</selected_tool_name>
</action>

Important:
- Respond ONLY with the two raw XML blocks.
- Do not use markdown code fences.
- Do not output multiple `<action>` blocks.
- Do not output more than one tool inside `<action>`.
- No additional commentary.
- If the previous step failed, revise the approach.
- Use interpolated variables like `$variable_name$` inside XML argument values when useful.
- Strictly follow the required arguments for each tool as defined in the system prompt.
- Use `<action><task_complete><answer>...</answer></task_complete></action>` to indicate task completion when the task is solved or deemed unfeasible.
