## Your task to solve:
<task>
{{ task }}
</task>

## Tool protocol
{{ tools_prompt }}

## Variable protocol
{{ variables_prompt }}

## Hard requirements
- If you use a tool, reply with raw XML only.
- Do not use markdown code fences.
- Output at most one `<action>` block.
- Inside `<action>`, output exactly one tool tag.
- Use only the exact argument names defined for that tool.
- When the task is complete, prefer calling `<task_complete>` with the final answer.

!! Remember each variable start with $ and end with $. Be very careful with variable interpolation.
